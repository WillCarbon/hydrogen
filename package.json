{
    "name": "themename",
    "version": "1.0.0",
    "description": "",
    "main": "web/index.php",
    "config": {
        "dir": "web/wp-content/themes/themename"
    },
    "scripts": {
        "setup": "npm i --prefix $npm_package_config_dir",
        "deploy": "composer install && npm run build",
        "watch": "npm run watch --prefix $npm_package_config_dir",
        "build": "npm run build --prefix $npm_package_config_dir",
        "update": "git pull && npm run build"
    },
    "private": true
}
